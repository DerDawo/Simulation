class environment:
    def __init__(self, name, xDimension, yDimension, populationAmmount, distance, hasMasks, hasAusgangssperre, hasLockdown):
        self.name = name
        self.xDimension = xDimension
        self.yDimension = yDimension
        self.populationAmmount = populationAmmount
        self.distance = distance
        self.hasMasks = hasMasks
        self.hasAusgangssperre = hasAusgangssperre
        self.hasLockdown = hasLockdown